use std::iter::{Peekable, Iterator};
use crate::iter::BlockingIter;
use crate::token::Token;
use crate::predicate;

// /// Parses a string literal
//fn parse_string_literal<I> (iterator: &mut Peekable<I>) -> Token where I: Iterator<Item=char> {
//
//}
